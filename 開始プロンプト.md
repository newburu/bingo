あなたは熟練したRuby on Railsのエンジニアです。
以下の要件に基づいて、ブラウザで動作する「ビンゴマシーン」のWebアプリを作成するための手順とコードを提示してください。
また、すべて日本語で対応してください。

## プロジェクトの概要
1から75までの数字をランダムに抽選し、履歴を表示するシンプルなビンゴマシーンアプリ。

## 技術スタック
* **Ruby:** 最新の安定版
* **Framework:** Ruby on Rails 最新の安定版
* **CSS:** Tailwind CSS（モダンで見やすいデザインにするため）
* **Database:** MySQL 最新の安定版
  
## Rails Gem
* **dotenv-rails** 環境変数を利用

## 機能要件
1.  **トップページ:**
    * 画面中央に大きく「現在の数字」を表示するエリアがある（初期状態は「START」などを表示）。
    * 「回す（SPIN）」ボタンがある。
    * 「リセット」ボタンがある。
2.  **抽選ロジック:**
    * 「回す」ボタンを押すと、1〜75の数字からランダムに1つ選ばれる。
    * **重要:** すでに既出の数字は選ばれないようにする。
    * すべての数字が出尽くしたら、「終了」と表示する。
3.  **履歴表示:**
    * 画面の下部または横に、これまでに出た数字の一覧を表示する。
    * 1〜75の数字一覧表があり、出た数字だけ色がつく形式だと望ましい。
    * 一覧は、Bの1〜15、Iの16〜30、Nの31〜45、Gの46〜60、Oの61〜75の形式で表示する。

## 実装ステップ
以下の手順で進めたいので、ステップごとにコードと実行コマンドを教えてください。
1.  Railsプロジェクトの作成とTailwind CSSのセットアップ
2.  モデルとコントローラーの作成（Game状態の保存など）
3.  ビューの作成（見た目の実装）
4.  ルーティングの設定

## デプロイ
1. Capistranoを使うこと
   
## 補足事項
1. プロジェクトディレクトリは今のディレクトリをそのまま利用する

初心者でもわかるように、ファイルの作成場所やターミナルでのコマンドを丁寧に説明してください。